# semantic_retrieval
This project implements a lightweight semantic search pipeline. Given a query sentence, the system encodes text using a transformer-based embedding model and returns the top-k most semantically similar sentences from the given data.
